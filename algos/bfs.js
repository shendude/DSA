/**
 * Breath first search visits all nodes in a level before starting to visit the next level
 * while DFS uses recursion/stack, BFS uses a queue. When we deque a node we queue its children
 */

 bfs = (root, target) => {
    return leaf;
}