/**
 * DFS (depth first search)
 * This algorithm concerns tree traversal. We first go as far as we can down 
 * a node and its leaves looking for the desired value. We then retrace back
 * if needed.
 */

dfs = (root, target) => {
    return leaf;
}